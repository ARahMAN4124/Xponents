import React from "react";
import { Grid } from "@material-ui/core";

const Card = ({ card }) => {
  return (
    <Grid item xs={12} sm={4}>
      <p>helleo boro vai ra</p>
    </Grid>
  );
};

export default Card;
